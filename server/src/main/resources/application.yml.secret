spring:
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: {client-id}
            client-secret: {slient-secret}
            scope:
              - profile
              - email